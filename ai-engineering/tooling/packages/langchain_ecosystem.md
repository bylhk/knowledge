# LangChain Ecosystem

**AI Framework**
| Tool | License | Stars | Purpose |
|------|---------|-------|---------|
| [LangChain](https://github.com/langchain-ai/langchain) | MIT | ~134k | The most widely adopted LLM orchestration framework with modular components for chains, agents, retrievers, tools, and memory. 1000+ integrations across model providers, vector stores, and tools. Reached v1.0 stability in October 2025. Split into `langchain-core` (base abstractions + LCEL), `langchain` (chains/agents), and `langchain-community` (third-party integrations). |
| [LangGraph](https://github.com/langchain-ai/langgraph) | MIT | ~10k | Low-level orchestration framework for building stateful, multi-step agents as cyclic graphs with nodes and edges. Built-in checkpointing (persistence, time-travel), human-in-the-loop via `interrupt()`, streaming, fault recovery, and durable execution. The recommended approach for all new agent implementations in the LangChain ecosystem. |
| [LangSmith](https://smith.langchain.com) | Proprietary | — | LangChain's native observability, evaluation, and deployment platform. Provides tracing (auto-instruments LangChain/LangGraph calls), prompt management, dataset creation, annotation queues, and online evaluation. Also powers LangGraph Platform (renamed "LangSmith Deployment" in Oct 2025) for production agent hosting. |
| [LangServe](https://github.com/langchain-ai/langserve) | Proprietary | ~2k | Deploys LangChain runnables/chains as REST APIs using FastAPI with auto-generated playground UI, input/output schemas, and streaming. Now in maintenance mode — LangGraph Platform is the recommended deployment path for new projects. |
| [LangChain Hub](https://smith.langchain.com/hub) | Proprietary | — | Community prompt registry hosted on LangSmith. Pull/push versioned prompts with `hub.pull("hwchase17/openai-functions-agent")`. Enables prompt sharing, versioning, and A/B testing across teams without code changes. |
| [LangGraph Studio](https://github.com/langchain-ai/langgraph-studio) | Proprietary | — | Visual desktop IDE for developing and debugging LangGraph agents locally. Visualises graph structure as Mermaid diagrams, lets you step through nodes, inspect state at each checkpoint, and test agent flows interactively before deployment. |

**Visual AI Workflows**
| Tool | License | Stars | Purpose |
|------|---------|-------|---------|
| [Langflow](https://github.com/langflow-ai/langflow) | MIT | ~45k | Visual drag-and-drop builder specifically for LangChain/LangGraph flows — each node maps to a LangChain component (models, prompts, retrievers, tools). Exports flows as Python code or deployable APIs. Good for prototyping RAG and agent pipelines visually before productionising. |
| [Flowise](https://github.com/FlowiseAI/Flowise) | Apache 2.0 | ~35k | JavaScript/TypeScript-based visual LLM flow builder using LangChain.js under the hood. Supports chatflows, agent flows, and tool integrations with one-click deployment. Lower barrier to entry with marketplace of pre-built templates and credential management. |
| [LangGraph Studio](https://github.com/langchain-ai/langgraph-studio) | Proprietary | — | Not a general visual builder but a debugging IDE for LangGraph specifically — visualise agent graph execution, inspect state transitions, replay from checkpoints, and identify failure points. Essential for developing complex multi-step agents. |

**Prompt Optimisation**
| Tool | License | Stars | Purpose |
|------|---------|-------|---------|
| [LangSmith Prompt Hub](https://smith.langchain.com/hub) | Proprietary | — | Version-controlled prompt registry with playground for iterating on prompts across models. Supports prompt variants, A/B testing, and team collaboration. Pull prompts at runtime with `hub.pull()` for hot-swapping without redeployment. |
| [LangSmith Datasets & Evaluation](https://docs.smith.langchain.com) | Proprietary | — | Create evaluation datasets, run prompts against them with automated scoring (LLM-as-judge, exact match, custom metrics), and compare results across prompt versions. Enables data-driven prompt iteration rather than vibes-based tuning. |
| [LCEL (LangChain Expression Language)](https://python.langchain.com/docs/concepts/lcel/) | MIT | (part of langchain-core) | Declarative composition syntax for chaining prompts, models, parsers, and retrievers with `|` pipe operator. Supports streaming, batch, async, and fallbacks out of the box. The foundation for building optimisable prompt pipelines where components can be swapped independently. |
| [Few-Shot Prompt Templates](https://python.langchain.com/docs/how_to/few_shot_examples/) | MIT | (part of langchain-core) | Dynamic example selection using semantic similarity (via vector store) or MMR to pick the most relevant few-shot examples per query. Enables automatic prompt optimisation by selecting contextually appropriate demonstrations rather than static examples. |

**Fine-Tuning (LoRA/GLoRA)**
| Tool | License | Stars | Purpose |
|------|---------|-------|---------|
| [LangSmith → Fine-Tuning Pipeline](https://docs.smith.langchain.com) | Proprietary | — | Export traced LangSmith conversations as fine-tuning datasets in OpenAI/chat format. Collect production LLM interactions, filter for high-quality examples via human annotation or automated scoring, then export for fine-tuning. Closes the loop between production observability and model improvement. |
| [langchain-openai (fine-tuning)](https://github.com/langchain-ai/langchain) | MIT | (part of ecosystem) | `ChatOpenAI(model="ft:gpt-4o-mini:...")` — seamlessly use fine-tuned OpenAI models as drop-in replacements within existing LangChain chains. No code changes beyond the model name. Same applies to Azure fine-tuned models via `AzureChatOpenAI`. |
| [langchain-google-vertexai (tuning)](https://github.com/langchain-ai/langchain) | MIT | (part of ecosystem) | Access Vertex AI tuned Gemini models through `ChatVertexAI`. Supports supervised fine-tuning and RLHF-tuned models deployed on Vertex AI endpoints with the same LangChain interface. |
| [langchain-huggingface (local fine-tuned)](https://github.com/langchain-ai/langchain) | MIT | (part of ecosystem) | Load locally fine-tuned models (LoRA/QLoRA adapters merged or via PEFT) through `HuggingFacePipeline` or `ChatHuggingFace`. Run your custom LoRA-adapted models within LangChain chains without any special handling — just point to the model path. |
| [LangChain + Unsloth/Axolotl workflow](https://github.com/langchain-ai/langchain) | MIT | — | Common pattern: collect high-quality chain outputs via LangSmith tracing → export as SFT dataset → fine-tune with Unsloth/Axolotl (LoRA/QLoRA/GLoRA) → deploy fine-tuned model back into LangChain via Ollama or HuggingFace integration. Documented in LangChain cookbook. |

**LLM Structured Output**
| Tool | License | Stars | Purpose |
|------|---------|-------|---------|
| [.with_structured_output()](https://python.langchain.com/docs/how_to/structured_output/) | MIT | (part of langchain-core) | Universal method on all LangChain chat models that constrains output to a Pydantic model, TypedDict, or JSON schema. Uses provider-native structured output (OpenAI JSON mode, Anthropic tool use, Gemini response schema) under the hood. The primary way to get reliable structured data from LLMs in LangChain. |
| [PydanticOutputParser](https://python.langchain.com/docs/how_to/output_parser_structured/) | MIT | (part of langchain-core) | Generates format instructions for the prompt and parses LLM text output into Pydantic objects with validation. Fallback approach for models that don't support native structured output. Includes retry logic with error feedback when parsing fails. |
| [JsonOutputParser](https://python.langchain.com/docs/how_to/output_parser_json/) | MIT | (part of langchain-core) | Streams and parses JSON from LLM output — supports partial JSON parsing for streaming scenarios. Works with any model that can produce JSON text without requiring native JSON mode support. |
| [XMLOutputParser](https://python.langchain.com/docs/how_to/output_parser_xml/) | MIT | (part of langchain-core) | Parses XML-structured output from LLMs — useful for models (like Claude) that are naturally good at XML generation. Supports nested structures and streaming partial results. |
| [OpenAI Functions/Tools](https://python.langchain.com/docs/how_to/tool_calling/) | MIT | (part of langchain-openai) | Leverages OpenAI's function/tool calling API for guaranteed JSON output matching a schema. `bind_tools()` attaches Pydantic models as tool definitions; the model is constrained to respond with valid tool calls. Works with `tool_choice="required"` for forced structured output. |
| [Enum/Categorical Output](https://python.langchain.com/docs/how_to/classify_text/) | MIT | (part of langchain-core) | Constrain LLM output to a fixed set of categories using Pydantic `Literal` types or `Enum` classes within `.with_structured_output()`. Guarantees classification results are always valid category values. |

**Ingestion (Document Loading & OCR)**
| Tool | License | Stars | Purpose |
|------|---------|-------|---------|
| [Document Loaders](https://python.langchain.com/docs/integrations/document_loaders/) | MIT | (part of langchain-community) | 160+ document loaders for every format — PDF (`PyPDFLoader`, `UnstructuredPDFLoader`), Word (`Docx2txtLoader`), HTML, CSV, JSON, Notion, Confluence, Google Drive, S3, web scraping (`WebBaseLoader`), and more. Each returns `Document` objects with content + metadata ready for splitting and embedding. |
| [langchain-unstructured](https://github.com/langchain-ai/langchain) | MIT | — | Integration with Unstructured.io for parsing 25+ file types with element-level extraction (titles, tables, images, narratives). `UnstructuredLoader` with `strategy="hi_res"` uses deep learning models for complex PDFs with tables and figures. The most capable document parser in the ecosystem. |
| [langchain-text-splitters](https://github.com/langchain-ai/langchain) | MIT | — | Chunking strategies: `RecursiveCharacterTextSplitter` (default), `TokenTextSplitter`, `MarkdownHeaderTextSplitter`, `HTMLHeaderTextSplitter`, `CodeTextSplitter` (language-aware for Python/JS/etc.), and `SemanticChunker` (embedding-based semantic boundaries). Supports parent-document and multi-vector retriever patterns. |
| [LlamaParse (via LangChain)](https://cloud.llamaindex.ai) | Proprietary | — | Managed document parsing service accessible through LangChain's `LlamaParseLoader`. Uses vision models for complex tables, charts, and multi-column layouts. Best-in-class for messy PDFs with nested tables. Free tier available. |
| [AmazonTextractPDFLoader](https://python.langchain.com/docs/integrations/document_loaders/amazon_textract/) | MIT | (part of langchain-aws) | AWS Textract integration for OCR + layout analysis of scanned documents. Extracts text, tables, forms, and signatures from images/PDFs with position information. Enterprise-grade OCR for production document processing pipelines. |
| [GoogleCloudVisionLoader](https://python.langchain.com/docs/integrations/document_loaders/) | MIT | (part of langchain-google-community) | Google Cloud Vision API integration for OCR on images and scanned documents. Supports handwriting recognition, document text detection, and multi-language extraction. |
| [Document Transformers](https://python.langchain.com/docs/integrations/document_transformers/) | MIT | (part of langchain-community) | Post-processing pipeline: `Html2TextTransformer`, `BeautifulSoupTransformer` (HTML cleaning), `DoctranQATransformer` (generate QA pairs), `DoctranPropertyExtractor` (extract metadata), and `EmbeddingsRedundantFilter` (deduplicate similar chunks). |

**RAG System (Two-Stage Retrieval & Re-Ranking)**
| Tool | License | Stars | Purpose |
|------|---------|-------|---------|
| [Vector Store Retrievers](https://python.langchain.com/docs/how_to/vectorstore_retriever/) | MIT | (part of langchain-core) | Unified retriever interface wrapping any vector store (Chroma, Pinecone, Qdrant, pgvector, FAISS, Milvus, Weaviate). Supports similarity search, MMR (maximal marginal relevance for diversity), and similarity score thresholds. The first stage in a typical two-stage RAG pipeline. |
| [langchain-chroma](https://github.com/langchain-ai/langchain) | MIT | — | ChromaDB integration — embedded vector store with metadata filtering. `Chroma.from_documents()` for quick setup; persistent storage for development. Good first-stage retriever for prototyping and small-medium collections. |
| [langchain-pinecone](https://github.com/langchain-ai/langchain) | MIT | — | Pinecone managed vector DB with serverless scaling, namespace isolation, and hybrid search (dense + sparse). Production-ready first-stage retriever with 99.99% SLA. |
| [langchain-qdrant](https://github.com/langchain-ai/langchain) | MIT | — | Qdrant vector DB integration with advanced payload filtering, sparse vectors, and quantisation. Excellent for production RAG needing complex metadata filters alongside semantic search. |
| [langchain-postgres (pgvector)](https://github.com/langchain-ai/langchain) | MIT | — | PostgreSQL + pgvector for keeping vectors alongside relational data. HNSW/IVFFlat indexes with SQL-based metadata filtering. Best when you already have Postgres and want to avoid a separate vector DB. |
| [BM25Retriever](https://python.langchain.com/docs/integrations/retrievers/bm25/) | MIT | (part of langchain-community) | Sparse keyword retrieval using BM25 — handles exact keyword matches that embeddings miss. Use in `EnsembleRetriever` alongside vector search for hybrid retrieval (first stage). |
| [EnsembleRetriever](https://python.langchain.com/docs/how_to/ensemble_retriever/) | MIT | (part of langchain) | Combines multiple retrievers (dense + sparse, multiple vector stores) with Reciprocal Rank Fusion (RRF) for hybrid search. The standard pattern for two-stage retrieval: BM25 + vector search → fused ranking before re-ranking. |
| [MultiQueryRetriever](https://python.langchain.com/docs/how_to/MultiQueryRetriever/) | MIT | (part of langchain) | Uses an LLM to generate multiple query variations from the original question, retrieves for each, and unions results. Improves recall by capturing different phrasings of the same intent. Good first-stage enhancement before re-ranking. |
| [ParentDocumentRetriever](https://python.langchain.com/docs/how_to/parent_document_retriever/) | MIT | (part of langchain) | Indexes small chunks for precise retrieval but returns the larger parent document for context. Solves the chunk-size dilemma: small chunks for matching accuracy, large chunks for LLM context. Two-tier storage pattern. |
| [ContextualCompressionRetriever](https://python.langchain.com/docs/how_to/contextual_compression/) | MIT | (part of langchain) | Second-stage re-ranking/filtering wrapper that compresses or re-ranks retrieved documents. Wraps any base retriever and applies a document compressor (cross-encoder re-ranker, LLM-based filter, or embeddings filter) to refine results. The standard LangChain pattern for two-stage retrieval + re-ranking. |
| [CohereRerank](https://python.langchain.com/docs/integrations/document_compressors/cohere_reranker/) | MIT | (part of langchain-cohere) | Production-grade neural re-ranker — wrap your retriever with `ContextualCompressionRetriever + CohereRerank` for state-of-the-art second-stage re-ranking. Single API call significantly improves retrieval quality. The most popular re-ranking choice in LangChain RAG pipelines. |
| [CrossEncoderReranker](https://python.langchain.com/docs/integrations/document_compressors/cross_encoder_reranker/) | MIT | (part of langchain-community) | Self-hosted cross-encoder re-ranking using sentence-transformers models (e.g., `cross-encoder/ms-marco-MiniLM-L-6-v2`). Second-stage re-ranker that runs locally without API costs — good balance of quality and control. |
| [FlashRank Reranker](https://python.langchain.com/docs/integrations/document_compressors/flashrank_reranker/) | MIT | (part of langchain-community) | Ultra-lightweight (~4MB) CPU-based re-ranker for resource-constrained environments. Millisecond re-ranking without GPU. Good second-stage option when latency and cost matter more than maximum quality. |
| [JinaRerank](https://python.langchain.com/docs/integrations/document_compressors/jina_reranker/) | MIT | (part of langchain-community) | Jina AI's neural re-ranker API with multi-language support and document-level relevance scoring. Alternative to Cohere for second-stage re-ranking with competitive quality. |
| [Self-Query Retriever](https://python.langchain.com/docs/how_to/self_query/) | MIT | (part of langchain) | Uses an LLM to extract structured filters from natural language queries (e.g., "papers from 2024 about transformers" → metadata filter `year=2024` + semantic search "transformers"). Combines structured filtering with semantic retrieval in a single step. |

**Evaluation**
| Tool | License | Stars | Purpose |
|------|---------|-------|---------|
| [LangSmith Evaluation](https://docs.smith.langchain.com/evaluation) | Proprietary | — | End-to-end evaluation platform: create datasets from production traces or manually, define evaluators (LLM-as-judge, exact match, custom Python), run experiments comparing prompt/model/chain variants, and track metrics over time. Supports pairwise comparison and annotation queues for human evaluation. The native evaluation solution for LangChain apps. |
| [langsmith.evaluation module](https://docs.smith.langchain.com/evaluation/how_to_guides/) | MIT | (part of langsmith SDK) | Python SDK for programmatic evaluation — `evaluate()` function runs your chain against datasets with custom evaluators. Built-in evaluators: `LabeledCriteria` (correctness, helpfulness), `EmbeddingDistance`, `StringDistance`, `JsonValidity`, and `RegexMatch`. |
| [RAG Evaluation (LangSmith)](https://docs.smith.langchain.com/evaluation) | Proprietary | — | Purpose-built RAG evaluators: faithfulness (is the answer grounded in retrieved context?), answer relevance (does it address the question?), context relevance (are retrieved docs relevant?), and context recall. Similar metrics to RAGAS but integrated into LangSmith's platform. |
| [LangSmith Online Evaluation](https://docs.smith.langchain.com/evaluation) | Proprietary | — | Real-time evaluation on production traces — attach evaluator rules that automatically score every trace (or a sample). Monitor quality drift, catch regressions, and alert on metric drops without manual test runs. |
| [Unit Testing Patterns](https://python.langchain.com/docs/how_to/) | MIT | (part of ecosystem) | Testing patterns for LangChain apps: mock LLM responses with `FakeListLLM`/`FakeMessagesListChatModel`, use `InMemoryVectorStore` for retriever tests, and assert chain outputs with pytest. Enables CI/CD testing without API calls. |

**LLM Efficiency**
| Tool | License | Stars | Purpose |
|------|---------|-------|---------|
| [Caching (InMemory/SQLite/Redis)](https://python.langchain.com/docs/how_to/llm_caching/) | MIT | (part of langchain-core) | Built-in response caching at the LLM layer — exact match (`InMemoryCache`, `SQLiteCache`, `RedisCache`) or semantic similarity (`GPTCache` integration). Avoids redundant API calls for identical/similar prompts. Set globally or per-chain. |
| [Rate Limiting](https://python.langchain.com/docs/how_to/chat_model_rate_limiting/) | MIT | (part of langchain-core) | Built-in rate limiter that throttles LLM calls to stay within provider limits. `ChatOpenAI(rate_limiter=InMemoryRateLimiter(requests_per_second=1))`. Prevents 429 errors without external rate-limiting infrastructure. |
| [Streaming](https://python.langchain.com/docs/how_to/streaming/) | MIT | (part of langchain-core) | First-class streaming support via `.stream()` and `.astream()` on all runnables. Stream tokens as they're generated, stream intermediate agent steps, and stream structured output chunks. Reduces time-to-first-token for better UX. |
| [Batch Processing](https://python.langchain.com/docs/how_to/parallel/) | MIT | (part of langchain-core) | `.batch()` and `.abatch()` for concurrent execution of multiple inputs with configurable `max_concurrency`. Automatic parallelisation of independent runnable branches in LCEL chains via `RunnableParallel`. |
| [Fallbacks](https://python.langchain.com/docs/how_to/fallbacks/) | MIT | (part of langchain-core) | `.with_fallbacks([backup_model])` for automatic failover between models/providers. If primary model fails (timeout, rate limit, error), transparently retries with backup. Essential for production reliability. |
| [Token Counting & Callbacks](https://python.langchain.com/docs/how_to/chat_token_usage_tracking/) | MIT | (part of langchain-core/community) | Track token usage per call with `get_openai_callback()` or model-native `usage_metadata`. Monitor costs, set token budgets, and optimise prompt length. Works with callbacks for real-time usage tracking. |
| [langchain-ollama (local inference)](https://github.com/langchain-ai/langchain) | MIT | — | Run models locally via Ollama for zero-cost inference during development. Same LangChain interface — swap `ChatOpenAI` → `ChatOllama` with no other code changes. Useful for testing and reducing API costs during iteration. |
| [Configurable Alternatives](https://python.langchain.com/docs/how_to/configure/) | MIT | (part of langchain-core) | `.configurable_alternatives()` to switch models at runtime via config without code changes. Route cheap queries to small models and complex queries to large models dynamically. |

**Guardrails**
| Tool | License | Stars | Purpose |
|------|---------|-------|---------|
| [Tool Input Validation (Pydantic)](https://python.langchain.com/docs/how_to/tools/) | MIT | (part of langchain-core) | All LangChain tools use Pydantic schemas for input validation — invalid tool calls are caught before execution. Define `args_schema` with validators, constraints, and descriptions. Prevents injection and malformed inputs at the tool boundary. |
| [Output Parsers + Retry](https://python.langchain.com/docs/how_to/output_parser_retry/) | MIT | (part of langchain-core) | `OutputFixingParser` and `RetryWithErrorOutputParser` catch malformed outputs and re-prompt the LLM with the error message. Self-healing chain pattern for structured output reliability without external guardrail frameworks. |
| [Moderation Chain](https://python.langchain.com/docs/how_to/) | MIT | (part of langchain-community) | `OpenAIModerationChain` wraps any chain with content moderation — checks inputs/outputs against OpenAI's moderation endpoint for hate, violence, sexual content, and self-harm. Blocks or flags harmful content automatically. |
| [Custom Validators (RunnableLambda)](https://python.langchain.com/docs/how_to/functions/) | MIT | (part of langchain-core) | Insert arbitrary validation logic anywhere in a chain using `RunnableLambda`. Check PII patterns, blocklists, length limits, or custom business rules. Raise exceptions to halt execution or route to fallback paths. |
| [LangGraph Guardrail Nodes](https://docs.langchain.com/oss/python/langgraph/) | MIT | (part of langgraph) | In LangGraph, implement guardrails as dedicated graph nodes — input validation node before LLM call, output validation node after. Conditional edges route to error handling or human review based on validation results. Most flexible guardrail architecture for complex agents. |
| [Guardrails AI Integration](https://python.langchain.com/docs/integrations/) | MIT | (part of langchain-community) | Integration with Guardrails AI framework — apply 200+ community validators (PII detection, toxicity, hallucination, competitor mentions) within LangChain chains. Automatic retry with validation feedback on failure. |
| [Amazon Bedrock Guardrails](https://python.langchain.com/docs/integrations/) | MIT | (part of langchain-aws) | AWS Bedrock Guardrails integration — managed content filtering, PII redaction, denied topic detection, and word filters applied to `ChatBedrock` calls. Enterprise-grade guardrails without self-hosting classifier models. |

**AI Footprint / Observability**
| Tool | License | Stars | Purpose |
|------|---------|-------|---------|
| [LangSmith Tracing](https://docs.smith.langchain.com/tracing) | Proprietary | — | Auto-instruments every LangChain/LangGraph call with detailed trace trees showing each step's input, output, latency, token usage, and cost. Drill into individual chain executions, identify slow nodes, and debug failures. Set `LANGSMITH_TRACING=true` and every call is captured. The native observability solution. |
| [LangSmith Monitoring](https://docs.smith.langchain.com/monitoring) | Proprietary | — | Production dashboards with aggregate metrics: P50/P95 latency, error rates, token usage trends, cost per user/chain, and throughput. Set up alerts for quality regression, latency spikes, or error rate increases. |
| [LangFuse Integration](https://langfuse.com/docs/integrations/langchain) | MIT | — | Open-source alternative to LangSmith — `CallbackHandler` that captures all LangChain calls with tracing, cost analytics, scoring, and prompt management. Self-hostable with clean UI. Use `langfuse.callback.CallbackHandler()` as a callback in any chain. |
| [Phoenix/Arize Integration](https://docs.arize.com/phoenix/integrations/langchain) | Elastic 2.0 | — | OpenTelemetry-based tracing for LangChain with embedding visualisation and retrieval quality debugging. Auto-instruments via `OpenInferenceTracer`. Good for teams wanting vendor-neutral observability with OTel export to Jaeger/Datadog/Grafana. |
| [OpenLLMetry/Traceloop](https://github.com/traceloop/openllmetry) | Apache 2.0 | ~3k | Auto-instruments LangChain calls as OpenTelemetry spans — export to any OTel-compatible backend. `Traceloop.init()` and all LLM/retriever/tool calls become spans automatically. Best for teams with existing OTel infrastructure (Grafana, Datadog, New Relic). |
| [Callbacks System](https://python.langchain.com/docs/how_to/callbacks_attach/) | MIT | (part of langchain-core) | Built-in callback system for custom observability — implement `on_llm_start`, `on_chain_end`, `on_tool_error`, etc. Write custom handlers for logging, metrics, alerting, or integration with proprietary monitoring systems. |
| [Weights & Biases (WandbTracer)](https://docs.wandb.ai/guides/integrations/langchain/) | Proprietary | — | W&B integration for experiment tracking LLM chains — logs traces, token counts, and outputs as W&B artifacts. Good for teams already using W&B for ML experiment management who want unified tracking. |

**AI UI**
| Tool | License | Stars | Purpose |
|------|---------|-------|---------|
| [Chainlit](https://github.com/Chainlit/chainlit) | Apache 2.0 | ~8k | Purpose-built chat UI for LangChain/LangGraph with native step visualisation, file handling, authentication, and multi-modal support. Shows intermediate agent steps (tool calls, retrieval, thinking) as expandable elements. `@cl.on_message` decorator integrates any LangChain chain in minutes. The default choice for LangChain-based chat apps. |
| [Streamlit (+ st.chat)](https://github.com/streamlit/streamlit) | Apache 2.0 | ~37k | Python-first web framework with `st.chat_message` and `st.chat_input` for building conversational UIs. Zero frontend knowledge required — wrap any LangChain chain with Streamlit's reactive state management. Huge community but limited customisation for complex chat layouts. |
| [Gradio (+ gr.ChatInterface)](https://github.com/gradio-app/gradio) | Apache 2.0 | ~35k | `gr.ChatInterface(predict_fn)` wraps any LangChain chain into a chat UI in one line. Supports streaming, file upload, audio input, and multimodal conversations. One-click deployment to Hugging Face Spaces for sharing demos. |
| [LangGraph Platform UI](https://docs.langchain.com/langsmith/) | Proprietary | — | Built-in agent UI provided by LangSmith Deployment — deploy a LangGraph agent and get a hosted chat interface with thread management, streaming, and interrupt handling. No custom frontend needed for basic agent interactions. |
| [Vercel AI SDK (+ LangChain)](https://sdk.vercel.ai/docs) | Apache 2.0 | ~10k | TypeScript/React SDK for building AI chat UIs with `useChat` hook that streams from LangChain.js backends. Supports tool call rendering, generative UI, and multi-step agent visualization. Best for production React/Next.js frontends backed by LangChain. |
| [CopilotKit](https://github.com/CopilotKit/CopilotKit) | MIT | ~15k | React framework for building AI copilots with LangGraph backend support. In-app chat, AI-assisted text editing, and agent-powered actions embedded directly into your application UI rather than standalone chat windows. |
